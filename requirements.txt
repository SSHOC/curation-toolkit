streamlit>=1.35
pandas
numpy
pyyaml
pillow
requests
fastparquet

# sshmarketplacelib is NOT installed from pip/GitHub here — it's vendored
# at vendor/sshmarketplacelib/ (loaded by lib/mplib.py) instead. See
# vendor/NOTICE.md for why: the upstream repo's default branch is not a
# stable install target (unpinned installs are not reproducible, and the
# repo bundles several GB of unrelated generated data alongside the
# ~100 KB library code, which pip would otherwise have to download in full).
